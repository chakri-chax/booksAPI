import logo from './logo.svg';
import './App.css';
import HomePage from './MyBooks';

function App() {
  return (
    <div className="App">
      <HomePage/>
    </div>
  );
}

export default App;

// const delay = (ms) => new Promise((res) => setTimeout(res, ms));